
  <div class="row">
    <div class="col s12">
      <div class="card-panel light-blue lighten-4">
        <div class="post-title"><a href='/profile/{{user_id}}' target="">@{{user.username}}</a> on {{format_date created_at}}</div>
        <div class="post-body"> <span class="white-text"><a href="/post/{{id}}" target="">{{title}}</a></div>
       
        </span>
        <span class="white-text">
        </span>
               <span class="white-text">
                     {{vote_count}} {{format_plural "like" vote_count}} with 
    <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
               </span>
      </div>
    </div>
  </div>
            